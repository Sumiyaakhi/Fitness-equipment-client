export type TBenefit = {
  name: string;
  benefit: string;
  img: string;
};
export type TTestimonial = {
  id: number;
  name: string;
  quote: string;
  imageUrl: string;
};
export type TUser = {
  name: string;
  email: string;
  phoneNumber: number;
  address: string;
};

export type TImage = {
  _id: number;
  img: string;
};
